#pragma once

#include <LovyanGFX.hpp>

namespace ui::radar {

/** Draw local airport runways on the radar grid (geographic scale). */
void drawRunways(lgfx::LGFXBase& gfx);

}  // namespace ui::radar
